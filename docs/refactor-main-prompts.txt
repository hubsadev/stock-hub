ETAPE 00 - Baseline propre

Verifie l'etat actuel du repo avant toute nouvelle extraction.

Actions:
- Lancer `git status --short --branch`.
- Verifier que les deplacements deja faits restent presents:
  - apps/web/src/components/layout/sidebar.html
  - apps/web/src/pages/login/login.html
  - apps/web/src/pages/login/render.ts
- Lancer `pnpm --filter @stock-hub/web typecheck`.
- Lancer `git diff --check`.
- Ne modifier aucun fichier fonctionnel dans cette etape.

Si tout est OK, faire un commit dedie:
`refactor(web): stabilize shell fragments`

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- `git status --short`

================================================================================

ETAPE 01 - Extraire les helpers UI HTML partages

Dans `apps/web/src/main.tsx`, deplace les helpers UI generiques lies au rendu HTML commun vers `apps/web/src/components/ui/html.ts`.

Fonctions visees:
- badge
- emptyRow
- actionEye
- actionEyeFor
- detailCard
- detailField
- lifecycleFields

Contraintes:
- Ne change ni le nom, ni la signature, ni le HTML genere.
- Si une fonction depend de `escapeHtml` ou `formatDate`, importe-les depuis les utils existants.
- Remplace les usages dans `main.tsx` par des imports.
- Ne deplace pas les fonctions metier ni les wrappers de pages.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- `git diff --check`
- Demarrer l'app et verifier rapidement tableaux, badges, drawers et modales.

Commit:
`refactor(web): extract shared html helpers`

================================================================================

ETAPE 02 - Extraire PWA et etat reseau

Dans `apps/web/src/main.tsx`, deplace la logique PWA/reseau dans `apps/web/src/services/pwa.ts`.

Fonctions/etat vises:
- deferredPwaInstallPrompt
- pwaServiceWorkerRegistered
- updatePwaInstallButton
- updateNetworkStatus
- installPwa
- offlineActionLabel
- requireOnlineAction
- cleanupDevelopmentPwa
- setupPwa
- updateProfilePwaCards

Contraintes:
- Ne change aucun comportement PWA.
- Le service worker reste desactive en dev.
- Les actions offline restent bloquees comme aujourd'hui.
- Utilise un contexte explicite si necessaire pour `showToast`, `setVisible`, `isOnline`.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- Demarrage dev: aucun bouton installation actif en dev.
- Build/preview si possible: PWA toujours disponible en production/preview.

Commit:
`refactor(web): extract pwa service`

================================================================================

ETAPE 03 - Extraire la page Profil

Dans `apps/web/src/template-parts/workspace.html`, identifie le bloc HTML de la page Profil (`section id="profil"` si present) et deplace-le dans `apps/web/src/pages/profil/profil.html`.

Dans `apps/web/src/main.tsx`, deplace la logique Profil vers `apps/web/src/pages/profil/render.ts`.

Fonctions visees:
- roleLabel
- accessLabel
- profileRoleBadge
- updateProfileView
- syncCurrentUser
- submitProfile
- submitPasswordChange

Contraintes:
- Garder `userIdentity`, `userDisplayName`, `userInitials` partages si encore utilises ailleurs, ou les extraire dans un service utilisateur commun uniquement si cela ne change rien.
- Conserver les wrappers dans `main.tsx`.
- Ne change aucun ID, data-action, texte, style ou comportement.
- La carte PWA du profil continue d'utiliser la logique PWA extraite.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- Ouvrir `/profil`.
- Modifier profil, changer mot de passe, verifier affichage sidebar/header.
- Verifier carte installation PWA en dev.

Commit:
`refactor(web): extract profil page`

================================================================================

ETAPE 04 - Extraire les actions header et navigation visuelle

Dans `apps/web/src/main.tsx`, deplace la logique d'actions d'en-tete et d'etat visuel de navigation vers `apps/web/src/components/layout/view-actions.ts`.

Fonctions visees:
- clearActiveNav
- activateNavButton
- renderHeaderButton
- canUseHeaderAction
- setViewActions

Contraintes:
- Ne change pas les actions disponibles par vue.
- Ne change pas les droits d'affichage.
- `showView`, `navigateToView` et `openRoute` restent dans `main.tsx` pour cette etape.
- Utilise un contexte explicite pour `currentUser`, `hasRole` et les helpers requis.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- Naviguer entre plusieurs pages.
- Verifier les boutons d'en-tete selon les roles.

Commit:
`refactor(web): extract view header actions`

================================================================================

ETAPE 05 - Extraire le controleur de modales

Dans `apps/web/src/main.tsx`, deplace la logique generique de modales vers `apps/web/src/app/modals.ts`.

Fonctions visees:
- openModal
- closeModal
- prepareTemplateActions

Contraintes:
- Conserver des wrappers `openModal`, `closeModal`, `prepareTemplateActions` dans `main.tsx`.
- Le module extrait recoit un contexte explicite avec toutes les callbacks de preparation existantes.
- Ne change aucun ID de modale, aucune condition, aucun ordre d'initialisation.
- Ne touche pas au HTML.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- Ouvrir chaque type de modale principale: referentiel, entree, sortie, retour, inventaire, user, equipement, vehicule.
- `git diff --check`

Commit:
`refactor(web): extract modal controller`

================================================================================

ETAPE 06 - Extraire les helpers formulaires et selects

Dans `apps/web/src/main.tsx`, deplace les helpers de formulaires/selects vers `apps/web/src/utils/forms.ts`.

Fonctions visees:
- fillSelect
- option
- userOptions
- articleOptions
- projectOptions
- clientOptions
- supplierOptions
- teamServiceOptions
- sitesForProject
- siteOptions
- setProjectSiteOptions
- locationOptions
- toNumber
- articleStockAtLocation si elle reste strictement utilitaire, sinon la garder jusqu'a extraction stock transverse.

Contraintes:
- Ne change pas les labels/options generes.
- Les fonctions qui dependent de donnees globales doivent recevoir ces donnees en parametre, avec wrappers conserves dans `main.tsx` si besoin.
- Aucun changement visuel.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- Tester selects dans Entrees, Sorties, Retours, Transferts, Referentiels, Equipements.
- `git diff --check`

Commit:
`refactor(web): extract form helpers`

================================================================================

ETAPE 07 - Extraire la logique mouvement partagee

Dans `apps/web/src/main.tsx`, deplace la logique transverse des mouvements vers `apps/web/src/services/movements.ts`.

Fonctions visees:
- movementTypeLabel
- movementTypeBadge si possible sans dependance UI, sinon separer label pur et garder badge wrapper
- movementQuantity
- movementActor
- movementArticleLabel
- movementStatus
- movementStatusLabel
- movementTextKey
- movementDateKey
- movementProjectKey
- movementPersonKey
- movementArticleKeys
- movementRequestedTotal
- movementCompletedTotal
- hasCommonArticle
- looksLikeGeneratedExit
- linkedExitForRequest
- requestForExit si elle peut etre parametree proprement

Contraintes:
- Ne change aucune regle metier.
- Les fonctions dependantes de `latestMovements` recoivent la liste en parametre.
- Conserver les wrappers dans `main.tsx` pour eviter de modifier tous les appels d'un coup.
- Les fonctions qui generent du HTML stylise peuvent rester wrappers dans `main.tsx`.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- Tester Historique, Sorties, Retours/Transferts.
- Verifier dedoublonnage demandes/sorties.
- `git diff --check`

Commit:
`refactor(web): extract movement logic`

================================================================================

ETAPE 08 - Extraire la logique preuves signees

Dans `apps/web/src/main.tsx`, deplace la logique transverse des preuves vers `apps/web/src/services/proofs.ts`.

Fonctions visees:
- movementProofSource
- movementProofCount
- movementHasProof
- movementRequiresSignedProof
- movementProofStatus
- proofRequestForMovement
- canUploadSignedProofFor si applicable

Contraintes:
- Ne change pas quels mouvements exigent une preuve.
- Entrees, sorties, retours et transferts continuent d'ouvrir la bonne preuve.
- Les fonctions d'action UI `viewSigned...` peuvent rester wrappers si elles dependent des pages.
- Aucun changement API.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- Historique: verifier `Jointe`, `Manquante`, `Non requise`.
- Voir preuve depuis Entrees, Sorties, Retours, Transferts.
- `git diff --check`

Commit:
`refactor(web): extract proof logic`

================================================================================

ETAPE 09 - Extraire les exports transverses

Dans `apps/web/src/main.tsx`, deplace la logique d'export commune vers `apps/web/src/services/exports.ts`.

Fonctions visees:
- csvValue
- toCsv
- downloadCsv si encore utilise
- excelCellText
- autoExcelColumnWidth
- exportWorkbook
- exportDateValue
- exportDataset
- exportRows
- exportData

Constantes visees:
- stockHubExcelBlue
- stockHubExcelBorder
- stockHubExcelStripe

Contraintes:
- Ne change pas le format des fichiers exportes.
- Les datasets qui dependent des pages doivent passer par un contexte explicite ou rester wrappers dans `main.tsx`.
- Les exports Vue Stock, Reappro, Inventaire, Audit doivent rester identiques.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- Exporter Vue Stock, Reapprovisionnement, Inventaire, Audit.
- Verifier que les fichiers restent `.xlsx` styles.
- `git diff --check`

Commit:
`refactor(web): extract export service`

================================================================================

ETAPE 10 - Extraire le rafraichissement API

Dans `apps/web/src/main.tsx`, deplace `updateApiBackedViews` vers `apps/web/src/app/data-refresh.ts`.

Contraintes:
- Le module extrait doit recevoir un contexte explicite contenant:
  - toutes les fonctions API appelees;
  - les setters des listes `latest...`;
  - les render callbacks;
  - `setText`, `isToday`, `window.lucide?.createIcons`.
- Ne change pas l'ordre logique des appels ni les catch existants.
- `main.tsx` garde un wrapper `updateApiBackedViews(root)`.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- Demarrer l'app et verifier chargement dashboard, referentiels, stock, historique, audit.
- Tester un refresh navigateur.
- `git diff --check`

Commit:
`refactor(web): extract data refresh orchestration`

================================================================================

ETAPE 11 - Extraire parsing et dispatch des actions

Dans `apps/web/src/main.tsx`, deplace `parseAction` vers `apps/web/src/app/actions.ts`.

Puis, seulement si le diff reste lisible, deplace aussi le dispatch click/change/input dans le meme module.

Contraintes:
- Commencer par `parseAction` uniquement.
- Ne change aucune chaine `data-action`.
- Ne change aucun type retourne par `parseAction`.
- Si le dispatch est trop risque, s'arreter apres `parseAction` et garder les listeners dans `main.tsx`.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- Cliquer sur actions principales: navigation, modales, submit, filtres, drawers.
- `git diff --check`

Commit:
`refactor(web): extract action parser`

================================================================================

ETAPE 12 - Nettoyage final de main.tsx

Nettoie `apps/web/src/main.tsx` sans changement fonctionnel.

Objectif:
- `main.tsx` doit devenir le point d'assemblage:
  - imports;
  - etat applicatif global restant;
  - context builders;
  - wrappers;
  - routing/session;
  - listeners React;
  - render ReactDOM.

Actions:
- Supprimer les lignes vides excessives.
- Regrouper les wrappers par domaine avec de courts commentaires si utile.
- Ne pas renommer les fonctions appelees par les handlers.
- Ne pas modifier la logique.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- `git diff --check`
- Demarrage app.
- Parcours rapide: login, dashboard, stock, entrees, sorties, inventaire, historique, profil.

Commit:
`refactor(web): tidy main app shell`

================================================================================

ETAPE 13 - Extraire les helpers utilisateur partages

Dans `apps/web/src/main.tsx`, deplace les helpers utilisateur partages vers `apps/web/src/services/users.ts`.

Fonctions visees:
- userIdentity
- userDisplayName
- userInitials
- roleLabel
- accessLabel

Contraintes:
- Ne change ni les noms, ni les signatures, ni les valeurs retournees.
- Conserver des wrappers dans `main.tsx` si des contextes/pages en dependent encore.
- Ne pas deplacer la logique Profil deja extraite, ni les fonctions de session/login.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- Verifier header/sidebar, Profil, Historique, Utilisateurs & roles.
- `git diff --check`

Commit:
`refactor(web): extract user helpers`

================================================================================

ETAPE 14 - Extraire le toast global

Dans `apps/web/src/main.tsx`, deplace `showToast` vers `apps/web/src/components/ui/toast.ts`.

Contraintes:
- Ne change pas le rendu, les classes, les durees, ni les tons.
- Garder un wrapper `showToast(root, message, tone)` dans `main.tsx`.
- Les pages continuent a recevoir `showToast` via leurs contextes.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- Tester une action succes, une erreur et une action bloquee offline.
- `git diff --check`

Commit:
`refactor(web): extract toast helper`

================================================================================

ETAPE 15 - Extraire les documents PDF partages

Dans `apps/web/src/main.tsx`, deplace la logique PDF/document encore centrale vers un module dedie.

Fichier cible recommande:
- `apps/web/src/services/documents.ts`

Fonctions visees:
- hubLogoMarkup
- materialRequestDocumentHtml
- downloadMaterialRequestPdf

Contraintes:
- Ne change pas le HTML/PDF genere.
- Ne change aucun libelle, signature, style ou logique de donnees.
- Si une fonction est uniquement liee aux sorties, elle peut etre exposee au contexte Sorties depuis ce service.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- Telecharger une fiche demande/sortie et comparer visuellement.
- `git diff --check`

Commit:
`refactor(web): extract document helpers`

================================================================================

ETAPE 16A - Preparer le service datasets d'export sans le brancher

Suite au rollback du commit `e693fc0`, ne pas refaire l'extraction export en une seule passe.

Dans cette etape, creer uniquement `apps/web/src/services/export-datasets.ts` avec les types et fonctions prepares, mais ne modifier aucun appel runtime dans `main.tsx`.

Objectif:
- Copier la logique de `exportDataset`, `exportRows` et `exportData` dans des fonctions parametrees.
- Ne pas brancher ces fonctions au dispatch ni aux wrappers existants.
- Garder `main.tsx` fonctionnel exactement comme avant.

Fonctions a preparer:
- exportDatasetFromContext
- exportRowsFromContext
- exportDataFromContext

Contraintes:
- Aucun comportement runtime ne doit changer dans cette etape.
- `main.tsx` peut importer seulement des types si necessaire, mais ne doit pas appeler le nouveau service.
- Ne pas toucher aux exports Vue Stock, Inventaire, Reapprovisionnement, Audit.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- `git diff --check`
- Demarrer l'app et verifier visuellement que rien n'a bouge:
  - KPI dashboard;
  - filtres Vue Stock;
  - filtres Historique;
  - ouverture/fermeture d'une modale.

Commit:
`refactor(web): prepare export datasets service`

================================================================================

ETAPE 16B - Brancher uniquement exportRows sur le service

Dans `apps/web/src/main.tsx`, brancher seulement le wrapper `exportRows(kind, root)` vers `exportRowsFromContext(...)`.

Contraintes:
- Ne pas brancher `exportDataset`.
- Ne pas brancher `exportData`.
- Garder les exports utilisateur actuels inchanges.
- Si `exportRows` n'est pas utilise runtime, cette etape doit rester sans effet visuel.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- `git diff --check`
- Demarrer l'app et verifier:
  - dashboard KPI;
  - modales avec donnees;
  - filtres qui gardent leur largeur normale;
  - fermeture des modales.

Commit:
`refactor(web): route export rows helper`

================================================================================

ETAPE 16C - Brancher exportDataset et exportData avec verification visuelle stricte

Brancher `exportDataset(kind, root)` et `exportData(root, kind)` vers le service `export-datasets.ts`.

Contraintes:
- Ne change ni noms de fichiers, ni colonnes, ni feuilles Excel, ni donnees exportees.
- Garder un wrapper `exportData(root, kind)` dans `main.tsx`.
- Apres branchement, lancer obligatoirement l'app en dev et verifier visuellement les ecrans principaux avant commit.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- `git diff --check`
- Verifier visuellement:
  - KPI dashboard non etires;
  - filtres Vue Stock et Historique normaux;
  - modales avec donnees;
  - boutons fermer fonctionnels;
  - export Reapprovisionnement;
  - export Audit;
  - exports Vue Stock et Inventaire inchanges.

Commit:
`refactor(web): extract export datasets`

================================================================================

ETAPE 17 - Extraire les badges et lignes HTML metier restantes

Dans `apps/web/src/main.tsx`, deplace les helpers HTML metier restants vers `apps/web/src/components/ui/stock-html.ts`.

Fonctions visees:
- stockStatus
- movementTypeBadge
- watchStockRow

Contraintes:
- Ne change pas le HTML genere.
- Importer `badge`, `escapeHtml`, `formatNumber` ou `formatDate` selon les besoins.
- Garder des wrappers dans `main.tsx` si les contextes existants les utilisent.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- Verifier Vue Stock, Dashboard, Historique, Entrees, Sorties.
- `git diff --check`

Commit:
`refactor(web): extract stock html helpers`

================================================================================

ETAPE 18 - Extraire le controleur shell

Dans `apps/web/src/main.tsx`, deplace la logique de shell applicatif vers `apps/web/src/app/shell-controller.ts`.

Fonctions visees:
- applyRoleAccess
- updateCurrentUserDisplay
- showView
- navigateToView
- openRoute

Contraintes:
- Conserver les wrappers dans `main.tsx`.
- Ne change aucune route, aucun alias, aucun droit d'acces, aucun bouton header.
- Le module recoit un contexte explicite pour `currentUser`, `canAccessView`, `setViewActions`, `showLogin`, `hideLogin`, `updateApiBackedViews`, `showToast`.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- Naviguer entre 5 ecrans.
- Tester F5 sur `/historiques`, `/vue-stock`, `/profil`.
- Tester logout/login.
- `git diff --check`

Commit:
`refactor(web): extract shell controller`

================================================================================

ETAPE 19 - Extraire le dispatch des actions

Dans `apps/web/src/main.tsx`, deplace progressivement le dispatch `onClick` vers `apps/web/src/app/dispatch.ts`.

Contraintes:
- Ne change aucune chaine `data-action`.
- Commencer par extraire une fonction `dispatchAction(root, parsed, target, ctx)`.
- Garder les listeners React dans `main.tsx`.
- Le contexte contient tous les handlers actuels appeles par le dispatch.
- Si le diff devient trop large, extraire seulement le dispatch click et laisser change/input dans `main.tsx`.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- Tester navigation, modales, filtres, drawers, submits, downloads et uploads.
- `git diff --check`

Commit:
`refactor(web): extract action dispatch`

================================================================================

ETAPE 20 - Nettoyage final apres dispatch

Nettoie `apps/web/src/main.tsx` apres extraction du dispatch.

Objectif:
- Garder uniquement:
  - imports;
  - etat global restant;
  - context builders;
  - wrappers indispensables;
  - listeners React;
  - render ReactDOM.

Actions:
- Supprimer les wrappers devenus inutiles.
- Regrouper les context builders.
- Supprimer les commentaires de section obsoletes.
- Ne pas modifier la logique.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- `git diff --check`
- Parcours rapide complet: login, dashboard, stock, entrees, sorties, retours, inventaire, historique, profil.

Commit:
`refactor(web): slim main shell`

================================================================================

ETAPE 21 - Baseline avant decoupage avance

Verifie l'etat actuel avant de continuer le decoupage de `main.tsx`.

Actions:
- Lancer `git status --short --branch`.
- Verifier que le dernier commit attendu existe:
  - `git log --oneline -5`
  - le commit `refactor(web): slim main shell` doit etre present.
- Lancer:
  - `pnpm --filter @stock-hub/web typecheck`
  - `git diff --check`
- Ne modifier aucun fichier dans cette etape.

Si tout est OK, ne fais pas de commit sauf s'il y a un changement strictement documentaire deja present.

Verification:
- repo propre;
- typecheck OK.

================================================================================

ETAPE 22 - Extraire les contextes globaux app

Dans `apps/web/src/main.tsx`, deplace les context builders globaux vers `apps/web/src/app/context-builders.ts`.

Fonctions ciblees:
- `pwaContext`
- `viewActionsContext`
- `modalControllerContext`
- `dataRefreshContext`
- `exportDatasetsContext`
- `shellControllerContext`
- `dispatchContext`

Contraintes:
- Ne change aucune logique.
- Ne change aucun nom de fonction visible cote `main.tsx`.
- Garder dans `main.tsx` des wrappers de meme nom qui appellent les builders extraits.
- Le nouveau fichier doit exposer des fonctions `createXContext(...)`.
- `main.tsx` reste proprietaire de l'etat global `latest...`, `currentUser`, `pendingRouteAfterLogin`.
- Les dependances doivent etre passees explicitement au module extrait.
- Ne pas toucher aux listeners React ni au dispatch.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- `git diff --check`
- Demarrer l'app.
- Verifier login, navigation, ouverture modale, filtre Historique, export Reappro.

Commit:
`refactor(web): extract app context builders`

================================================================================

ETAPE 23 - Extraire les contextes pages simples

Dans `apps/web/src/main.tsx`, deplace les context builders des pages simples vers leurs dossiers respectifs.

Fonctions ciblees:
- `tableauDeBordContext`
- `reapprovisionnementContext`
- `auditAlertesContext`
- `historiqueContext`
- `utilisateursRolesContext`
- `vueStockContext`

Fichiers cibles:
- `apps/web/src/pages/tableau-de-bord/context.ts`
- `apps/web/src/pages/reapprovisionnement/context.ts`
- `apps/web/src/pages/audit-alertes/context.ts`
- `apps/web/src/pages/historique/context.ts`
- `apps/web/src/pages/utilisateurs-roles/context.ts`
- `apps/web/src/pages/vue-stock/context.ts`

Contraintes:
- Chaque fichier exporte `createXContext(...)`.
- `main.tsx` garde des wrappers `xContext()` de meme nom.
- Aucun comportement runtime ne change.
- Ne pas modifier les fichiers `render.ts` sauf import type-only si necessaire.
- Ne pas deplacer les actions dans cette etape.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- `git diff --check`
- Verifier visuellement Dashboard, Reappro, Audit, Historique, Users, Vue Stock.

Commit:
`refactor(web): extract simple page contexts`

================================================================================

ETAPE 24 - Extraire les contextes pages complexes

Dans `apps/web/src/main.tsx`, deplace les context builders des pages complexes vers leurs dossiers respectifs.

Fonctions ciblees:
- `referentielsContext`
- `entreesStockContext`
- `sortiesStockContext`
- `retoursTransfertsContext`
- `inventaireStockContext`
- `equipementsContext`
- `parcAutoContext`
- `profilContext`
- `loginContext`

Fichiers cibles:
- `pages/referentiels/context.ts`
- `pages/entrees-stock/context.ts`
- `pages/sorties-stock/context.ts`
- `pages/retours-transferts/context.ts`
- `pages/inventaire-stock/context.ts`
- `pages/equipements/context.ts`
- `pages/parc-auto/context.ts`
- `pages/profil/context.ts`
- `pages/login/context.ts`

Contraintes:
- Extraction structurelle uniquement.
- `main.tsx` conserve les fonctions `xContext()` comme wrappers.
- Les setters `latest...` restent dans `main.tsx`.
- Ne pas changer les signatures des contextes attendus par `render.ts`.
- Ne pas modifier les regles metier, preuve, stock, droits ou routing.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- `git diff --check`
- Tester Entrees, Sorties, Retours, Inventaire, Referentiels, Profil.

Commit:
`refactor(web): extract complex page contexts`

================================================================================

ETAPE 25 - Creer des action groups par domaine sans branchement runtime

Prepare l'architecture d'actions par domaine, mais sans modifier le dispatch runtime.

Creer des fichiers:
- `pages/entrees-stock/actions.ts`
- `pages/sorties-stock/actions.ts`
- `pages/retours-transferts/actions.ts`
- `pages/inventaire-stock/actions.ts`
- `pages/referentiels/actions.ts`
- `pages/historique/actions.ts`
- `pages/equipements/actions.ts`
- `pages/parc-auto/actions.ts`
- `pages/utilisateurs-roles/actions.ts`
- `pages/profil/actions.ts`

Objectif:
- Chaque fichier expose une fonction `createXActions(ctxDeps)` preparee.
- Ne pas appeler ces actions depuis `main.tsx`.
- Ne pas modifier `dispatch.ts`.
- Copier uniquement des wrappers existants sous forme preparee.

Contraintes:
- Aucun comportement runtime ne doit changer.
- Aucun `data-action` ne change.
- Si une action depend de `root`, elle doit garder `root` en parametre.
- Les fichiers peuvent rester inutilises dans cette etape.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- `git diff --check`
- Demarrage app et verification rapide qu'aucun comportement n'a change.

Commit:
`refactor(web): prepare page action groups`

================================================================================

ETAPE 26 - Brancher les actions simples au dispatch

Dans `main.tsx`, remplace progressivement une partie de `dispatchContext()` par les action groups prepares.

Domaines a brancher dans cette etape:
- Profil
- Utilisateurs & roles
- Historique
- Audit
- Vue Stock
- Reapprovisionnement

Contraintes:
- Ne pas brancher Entrees, Sorties, Retours, Inventaire ni Referentiels dans cette etape.
- Garder les wrappers existants dans `main.tsx` si un autre contexte en depend encore.
- Ne pas modifier `parseAction`.
- Ne pas modifier les listeners React.
- Le dispatch doit continuer a recevoir exactement les memes handlers.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- `git diff --check`
- Tester:
  - Profil;
  - Users;
  - Historique;
  - Audit;
  - Vue Stock;
  - Reapprovisionnement.

Commit:
`refactor(web): route simple page actions`

================================================================================

ETAPE 27 - Brancher les actions Referentiels et Inventaire

Dans `main.tsx`, branche les action groups prepares pour:
- Referentiels
- Inventaire

Contraintes:
- Ne change aucun import Excel.
- Ne change aucun modele Excel/PDF.
- Ne change aucun `data-action`.
- Garder les wrappers encore utilises par les autres pages.
- Ne pas toucher aux actions Entrees/Sorties/Retours dans cette etape.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- `git diff --check`
- Tester:
  - onglets Referentiels;
  - creation element;
  - import Excel referentiel;
  - import inventaire;
  - comptage inventaire;
  - export inventaire.

Commit:
`refactor(web): route referentials inventory actions`

================================================================================

ETAPE 28 - Brancher les actions Entrees, Sorties, Retours

Dans `main.tsx`, branche les action groups prepares pour:
- Entrees stock
- Sorties stock
- Retours & transferts

Contraintes:
- C'est une etape sensible: ne modifier que le wiring d'actions.
- Ne change aucun endpoint API.
- Ne change aucune regle preuve/PDF.
- Ne change aucune regle de dedoublonnage sortie/demande.
- Garder les wrappers necessaires aux contextes partages.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- `git diff --check`
- Tester:
  - creation entree;
  - detail entree;
  - creation demande sortie;
  - preparation sortie;
  - sortie directe;
  - creation retour;
  - creation transfert;
  - upload/voir preuve.

Commit:
`refactor(web): route movement page actions`

================================================================================

ETAPE 29 - Supprimer les wrappers rendus inutiles par les action groups

Dans `main.tsx`, supprime uniquement les wrappers devenus inutiles apres branchement des action groups.

Methode obligatoire:
- Pour chaque wrapper candidat, lancer:
  - `rg -n "\\bNOM_FONCTION\\b" apps/web/src`
- Supprimer uniquement si:
  - il n'est plus appele dans `main.tsx`;
  - il n'est plus passe a un contexte;
  - il n'est plus utilise par `dispatchContext`;
  - il n'est plus utilise par un fichier `actions.ts`, `context.ts`, `render.ts` ou service.

Contraintes:
- Ne pas supprimer les wrappers encore utilises comme facade transversale.
- Ne pas renommer les fonctions restantes.
- Ne pas modifier la logique.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- `git diff --check`
- Parcours rapide complet de l'application.

Commit:
`refactor(web): remove obsolete main wrappers`

================================================================================

ETAPE 30 - Extraire les listeners React hors du corps du composant

Dans `apps/web/src/main.tsx`, deplace la logique d'attachement des listeners vers `apps/web/src/app/listeners.ts`.

Fonctions ciblees:
- logique `onClick`
- logique `onChange`
- logique `onInput`
- logique scroll/resize/popstate
- cleanup associe

Contraintes:
- `main.tsx` garde le composant React et appelle une seule fonction:
  - `setupAppListeners(root, ctx)`
- Ne change aucun comportement.
- Ne change aucun listener ni ordre d'enregistrement.
- Ne change pas `dispatchAction`.
- Ne change pas les handlers appeles.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- `git diff --check`
- Tester:
  - navigation;
  - changement filtres;
  - recherche input;
  - import fichier;
  - scroll fermeture menu sorties;
  - F5 / popstate.

Commit:
`refactor(web): extract app listeners`

================================================================================

ETAPE 31 - Transformer `main.tsx` en bootstrap minimal

Nettoie `main.tsx` apres extraction des contextes, actions et listeners.

Objectif final:
- `main.tsx` contient uniquement:
  - imports principaux;
  - etat global restant;
  - wrappers indispensables restants;
  - creation des contextes via modules;
  - composant `StockHubTemplate`;
  - `ReactDOM.createRoot`.

Contraintes:
- Ne pas supprimer un wrapper sans recherche `rg`.
- Ne pas modifier la logique.
- Ne pas modifier les routes.
- Ne pas modifier les `data-action`.
- Ne pas lancer de formatter global automatique.

Verification:
- `pnpm --filter @stock-hub/web typecheck`
- `git diff --check`
- Demarrer l'app.
- Parcours complet:
  - login/logout;
  - dashboard;
  - Vue Stock;
  - Entrees;
  - Sorties;
  - Retours/transferts;
  - Inventaire;
  - Historique;
  - Profil;
  - Referentiels;
  - Audit;
  - Users.

Commit:
`refactor(web): minimize main bootstrap`
